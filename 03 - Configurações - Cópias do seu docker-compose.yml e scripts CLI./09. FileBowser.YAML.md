version: '3'
services:
  filebrowser:
    container_name: filebrowser
    image: filebrowser/filebrowser:latest
    ports:
      - '8081:80'
    volumes:
      - /home/serv/documentos:/srv
      - /home/serv/filebrowser/filebrowser.db:/database.db  # Mudança aqui
    # Removido o settings.json por agora para simplificar e evitar erros
    restart: always